#include <stdio.h>

int main(){

    int a,b,c;

    a = 10;
    b = 20;

    a += 90;

    printf("Answer = %d", (a = b)||(a < b));

    return 0;
} 

//(a = b) || (a < b): นิพจน์ทั้งหมดนี้ใช้ตัวดำเนินการตรรกะ OR เพื่อรวมผลลัพธ์ของสองนิพจน์ย่อยก่อนหน้า มันจะคืนค่าเป็น 1 ถ้า (a = b) หรือ (a < b) อยู่ในสถานะจริง

// คำสั่ง printf จะพิมพ์ "Answer = 1" ถ้า a = b หรือ a < b เป็นจริง ถ้าทั้งสองเป็นเท็จ มันจะพิมพ์ "Answer = 0" 